import { StyleSheet } from "react-native";

const CategoryModalStyle = StyleSheet.create({
  modalScreen: {
    alignItems: "center",
  },
  categories: { width: "100%", height: 500 },
  button: {
    marginBottom: 20,
    width: "90%",
  },
});

export default CategoryModalStyle;
